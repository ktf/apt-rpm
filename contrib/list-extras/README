This script will list all installed packages which are not
availabe in any online repository.

This script must be plugged in the slot

    Scripts::AptCache::Command

To use it, just run

    apt-cache list-extras
